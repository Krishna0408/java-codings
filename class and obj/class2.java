import java.util.*;
class employee{
    void loop(String name,int id){
        System.out.println(id+" "+name);
    }
}
public class class2 {

    public static void main(String[] args) {
        employee e=new employee();
        Scanner sc=new Scanner(System.in);
        String name=sc.next();
        int id=sc.nextInt();
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
        e.loop(name,id);
    }
}